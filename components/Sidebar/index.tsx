import BaseSpinner from "../LoadingIndicator/BaseSpinner";

const Sidebar = () => {
  return (
    <aside className="sidebar-page bg-error">
      Day la Sidebar
      <div></div>
    </aside>
  );
};

export default Sidebar;
